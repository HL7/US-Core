<!-- ================================================================
 The following Provenance list is generated by the liquid script below.
 The source data is at temp/pages/_data/structuredefinitions.json
 which is automatically generated by the build

The "black_list" below is manually updated as needed when new profiles are added to the guide.
====================================================================== -->

{% assign black_list = "Extension,Provenance,Medication,Organization,Practitioner,PractitionerRole,Location,Specimen,Questionnaire" | split:"," -%}
{% assign resource_list = "" -%}
{%- for sd_hash in site.data.structuredefinitions -%}
  {%- assign sd = sd_hash[1] -%}
  {%- unless black_list contains sd.type -%}
       {% assign resource_list = resource_list | append: sd.type | append: "," -%}
  {%- endunless -%}
{%- endfor -%}
<ul>
<li> *The US Core Provenance resource <strong>SHALL</strong> be supported for these US Core resource types:
<ul id="prov-white-list">
{% assign resource_list = resource_list | split: "," | uniq | sort -%}
{% for r in  resource_list %}
    <li>{{r}}</li>
{% endfor %}
</ul>
</li>
</ul>

<!-- ================================================ -->